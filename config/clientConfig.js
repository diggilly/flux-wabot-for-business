// clientConfig.js
export const clientConfig = {
    // Example configuration for the client, add or modify based on your setup
    puppeteer: {
      headless: true, // Set to true for headless mode
      args: ['--no-sandbox', '--disable-setuid-sandbox'],
    },
  };
  
